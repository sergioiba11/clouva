# CLOUVA avatar model

Replace this placeholder path with the production GLB:

- `public/models/clouva-avatar.glb`

The home experience is already configured to load that file and falls back to a lightweight CSS avatar if the GLB is not present.
