import { ProShell } from "@/components/pro-shell";
import { SectionBlock } from "@/components/section-block";

export default function UniversoPage() {
  return (
    <ProShell>
      <SectionBlock
        eyebrow="Universo"
        title="Narrativa CLOUVA"
        description="Espacio para manifestos, drops narrativos y contenido editorial conectado a Supabase (tabla drops + posts)."
      />
    </ProShell>
  );
}
