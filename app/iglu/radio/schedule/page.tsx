import { RadioSectionPage } from "@/components/iglu-radio/RadioSectionPage";

export default function IgluRadioSchedulePage() {
  return (
    <RadioSectionPage
      eyebrow="05 / SCHEDULE"
      title="PROGRAMACIÓN"
      description="Hoy, mañana y la semana de IGLÚ RADIO, preparada para estados LIVE, UP NEXT, SCHEDULED y ENDED basados en datos reales."
      note="Aún no hay una grilla publicada; por eso no mostramos horarios ficticios."
    />
  );
}
