# Clean PR baseline (from local main snapshot)

Este branch recompone un PR limpio desde el snapshot local de `main`, manteniendo:

- Tienda pública
- Admin
- Mi Flow
- Migraciones Supabase
- Configuración Vercel/Next
- Theme dark/light
- Diseño premium CLOUVA

## Validación local ejecutada

- `npm install` (no finaliza en este entorno por limitación de red/proxy)
- `npm run build` (falla porque no termina instalación de dependencias)
